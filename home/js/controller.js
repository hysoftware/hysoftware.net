/*global angular*/
(function (angular) {
    "use strict";
    angular.module("hysoft.home.controllers", [
    ]).controller("homeController", [
        "$scope",
        function () {
            // Add code and remove return undefined
            return undefined;
        }
    ]);
}(angular));
